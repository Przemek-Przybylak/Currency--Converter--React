import "./style.css";

const Section = () => (
    <section className="section">
        <span className="section__description">Wynik to: </span>
        <strong className="section__result "></strong>
    </section>

);

export default Section;